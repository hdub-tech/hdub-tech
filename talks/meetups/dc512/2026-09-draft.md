1. Whoami
2. friend request
   1. “Tool idea if you want to run with it”
   2. I’m still friends with a bunch of the current and former pentesters at Rapid7 and they know that when I left I told them they could ping me with tool ideas or feature/bug fix ideas for existing open source tools and I would check it out and see if it was something I could take on. Three years later, right when I was done with free time and trying to do a job hunt, someone was finally taking me up on this.
3. The idea / claim: “Wordpress XMLRPC interface allows for submitting a f*** ton of auth in one post request. And far as i can tell, no one has made a tool that you can use to just feed a username and password list and just send one bulk auth”
   1. now before you know-it-alls roll your eyes and immediately know a tool, I know, we will talk about that at the end. And if we don’t, come talk to me after so I can write it down
4. Breaking it down
   1. Wordpress – 
      1. Blogging software
      2. please note this was my first time even LOOKING at WordPress
      3. I only spent a little over a week with it
      4. I am not a WordPress expert, if you ask me a question about it, I probably will NOT know the answer
   2. XMLRPC
      1. legacy endpoint... existed from the beginning of time
      2. unauthenticated HTTP requests with an XML body
      3. Designed for programs and not humans, 2fa not supported natively
      5. enabled by default since 3.5
      6. toss a wp.getUsersBlogs since 2.6
          1. cool thing about this method is you only need to send a username/password
   3. multicall
      0. Lets you make a wholeeeeeee bunch of requests in a SINGLE POST. great way to test a whole bunch of usernames/passwords at once without a metric ton of requests going across the network or at an instance.
         1. multicall call to getUsersBlogs
      1. deliberately leaving a piece of information out about the vulnerability of this - so IYKYK don't blurt it out yet, we will all learn in the same order I did today.
5. back to the claim
   0. I don't like to reinvent the wheel, unless the wheel is square - so I'm not going to just take friend's claim at face value, I'm going to try to find a tool for this
   1. they already sent me to hacktricks - and right on it, it lists wpxploit. takes a user! takes a wordlist! *my work here is done"
      1. Friend goes to run it, but it doesn't take a user list. OK my bad, I go to fork it so I can add the user list functionality but on looking a little closer – this is only doing brute force, NOT utilizing multicall. I thought about just adding that too, but I decided to search a little harder.
   2. !!!! SPOILERS wpscan does do this, so does metasploit, and while I did look at the former, I didn't look close enough, probably cuz I was too excited to have something to do. The latter I just totally forgot to check, which is ironic for reasons 2 of you might get. I discovered that both of these did it while putting these slides together last week - but  we will talk about both more at the end. !!!!
   3. SOMEHOW I ended up looking at the xmlrpc-bruteforcer tag. A couple of these had promise - but trixsec/brute-xmlrpc was closest, and was updated last year even. Most of the things 
       1. Already has support for checking xmlrpc availability, username files, password files, multicall suppoort, user enumeration, outputs successful user/pass combos to successful_logins.json file, ships with a default wordlist, Tor support, WAF detection ...but does it work?
6. Time for a test environment!
   1. Not gonna burn my buddy again this time... so I went to test it for myself.
   2. stumbled on github.com/vulhub/vulhub – had 2 wordpress versions, but little things 
   3. ended up just copying the wordpress compose files off of the official docker containers pages
      1. note you have to do the click thru to setup the initial user, and there is a way to automate that and the vulhub did do it
   4. Great I'm good to go - I've got WordPress 7 running. some of you have you probably already giggled at this version, the rest of you get to suffer in order like I did.
7. Confirming the tool works
   1. git clone TrixSec/Brute-XMLRPC
   2. Create a small username file with just the existing user in my test instance, ditto a password file with just the existing password.
   3. The projct README lists some Required Python packages and separately lists a pip install command with slightly different package list, so I start with the short list
   4. Run tool - immediately fails with modules not found. It is at this point I fork the project and go straight to fixing it. First commit: now has a proper requirements file and README is just a `pip install -r requirements.txt`
   5. Run the tool again - looooooks like it works. Hacktricks wiki said to expect to see the `isAdmin` part in the response for working users/passwords, but the tool  isn't outputting the expected `MATCH: user:pass` and there isn't a successful_logins.json file. OK time to code dive.
8. Fixing it for 1 username / 1 password
   1. Looking at the code, it looks like it was parsing success based on a key of `Dashboard` not `isAdmin` - my first thought was this must be for an earlier version (if you know, please tell me after I really want to know) - and going with the assumption that this worked in The Past, so I left that check and added an additional one for isadmin. cool works, now outputs Match to the console... but still no json updates
   2. HOW UPDATED JSON
   3. ok we're done right? tell friend and PR to creator yes?
9. start PR
   1. qa background. love pr template to keep me honest. used one of my own since none here
   2. regression testing? never heard of her.
  
defenders
- Advanced Admin Handbook Security section, has hardening : https://developer.wordpress.org/advanced-administration/security/ 
- disable xmlrpc endpoint, restrict it with WAF rules, rate limit
- WP core does not support 2FA since 2025, but there are plugins to support it.
- Configure SSO/IdP
